% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hwsd_extract.R
\name{hwsd_compose}
\alias{hwsd_compose}
\title{Synthesize HWSD v2.0 attributes using per-variable aggregation rules}
\usage{
hwsd_compose(
  coords = NULL,
  bbox = NULL,
  param = "ALL",
  layer = "D1",
  path = tempdir(),
  ws_path = file.path(tempdir(), "ws_db"),
  internal = TRUE,
  tiles_deg = Inf,
  cores = 1,
  verbose = FALSE,
  props = hwsd_props(),
  output = "wide"
)
}
\arguments{
\item{coords}{A point c(lon, lat), matrix (2 cols), or data.frame with lon/lat.}

\item{bbox}{A bbox c(lon_min, lat_min, lon_max, lat_max); \code{sf} bbox also accepted.}

\item{param}{Character vector of property names; \code{"ALL"} selects all available
attributes. For \code{bbox} queries, non-numeric columns are automatically dropped
when \code{param = "ALL"}.}

\item{layer}{Soil layer code ("D1"-"D7"). For \code{bbox}, must be length 1.}

\item{path}{Output path when writing raster (used if \code{internal = FALSE}).}

\item{ws_path}{Path to HWSD index grid; will be downloaded if missing.}

\item{internal}{If \code{TRUE}, return in-memory raster; if \code{FALSE}, write to \code{path}.}

\item{tiles_deg}{Optional tiling size (degrees) for large bboxes; when finite and
smaller than extent, tiles are processed and mosaicked.}

\item{cores}{Number of cores for tiling (uses \code{parallel::mclapply} on non-Windows).}

\item{verbose}{Show progress messages.}

\item{props}{A tibble/data.frame from \code{hwsd_props()} (or a named vector of
aggregation methods). Modify the \code{agg} column to control synthesis. If a
\code{precision} column is present, it is used to round numeric outputs.}

\item{output}{Output shape for point queries: \code{"wide"} or \code{"long"}.}
}
\value{
Same as \code{hwsd_extract()}.
}
\description{
Synthesize HWSD v2.0 attributes using per-variable aggregation rules
}
\examples{
\dontrun{
props <- hwsd_props()
# Share-weighted synthesis with default rules
pt_syn <- hwsd_compose(
  coords = c(110, 40),
  param = c("SAND", "PH_WATER"),
  layer = "D1",
  ws_path = "D:/data/HWSD2",
  props = props
)
}
}
